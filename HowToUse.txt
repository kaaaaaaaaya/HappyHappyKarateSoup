# JAVA_HOME を 17 に強制上書き
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-17.0.18.8-hotspot" 

# Path の最優先順位に 17 を持ってくる
$env:Path = "$env:JAVA_HOME\bin;$env:Path"

# 確認（ここで 17 が出れば勝ち！）
javac -version

mvn clean

mvn spring-boot:run

#別のターミナルを作る
npm run dev